#!/usr/bin/env ruby

require_relative '../lib/move.rb'

board = [" ", " ", " ", " ", " ", " ", " ", " ", " "]
# Code your CLI Here"
puts"Welcome to Tic Tac Toe!"
puts"Where would you like to go?"

indexStr = gets.strip
input_to_index = indexStr.to_i
char = "X"
board = move(board,(index-1),char)
display_board(board)
